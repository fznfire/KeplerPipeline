import numpy as np
import os

#Make the copy of the csv file

#Open one file at a time, and allow changing the status
Status = np.loadtxt(outputpath+SubFolder+".csv",delimiter=',',skiprows=1,dtype=int)

Make
